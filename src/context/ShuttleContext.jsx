// import React, { createContext, useState } from 'react';
// import { mockShuttleData } from '../utils/mockData'; // import the mock data

// export const ShuttleContext = createContext();

// const ShuttleProvider = ({ children }) => {
//   const [shuttleData, setShuttleData] = useState(mockShuttleData);

//   return (
//     <ShuttleContext.Provider value={{ shuttleData }}>
//       {children}
//     </ShuttleContext.Provider>
//   );
// };

// export default ShuttleProvider;
